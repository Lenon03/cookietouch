export default class PaddockInformations {
  public maxOutdoorMount: number;
  public maxItems: number;
  constructor(maxOutdoorMount = 0, maxItems = 0) {

    this.maxOutdoorMount = maxOutdoorMount;
    this.maxItems = maxItems;

  }
}
