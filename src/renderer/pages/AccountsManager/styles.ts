import { StyleRulesCallback } from "@material-ui/core/styles/withStyles";

export type AccountsManagerStyle = "root";

export const accountsManagerStyles: StyleRulesCallback<
  AccountsManagerStyle
> = theme => ({
  root: {
    flexGrow: 1
  }
});
