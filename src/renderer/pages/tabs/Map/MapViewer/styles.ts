import { StyleRulesCallback } from "@material-ui/core/styles/withStyles";

export type MapViewerStyle = "root";

export const mapViewerStyles: StyleRulesCallback<MapViewerStyle> = theme => ({
  root: {
    flexGrow: 1
  }
});
